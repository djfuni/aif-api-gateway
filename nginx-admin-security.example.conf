# Optional Nginx hardening. Add inside your server block if needed.

location ~* ^/admin/(?:_auth\.php|.*\.local\.php|.*\.ini|.*\.log)$ {
    deny all;
    return 403;
}

location ^~ /admin/ {
    index index.php;
    try_files $uri $uri/ /admin/index.php?$query_string;
}
