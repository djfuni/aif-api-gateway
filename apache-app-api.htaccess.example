# Optional Apache hardening snippet. Put these lines in the site root .htaccess if your host allows it.
SetEnvIf Authorization "(.+)" HTTP_AUTHORIZATION=$1

<Files "app_api.php">
  Require all granted
</Files>

<FilesMatch "\.(json|key|log)$">
  Require all denied
</FilesMatch>
